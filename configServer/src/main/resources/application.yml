server:
  port: 9090
  
    
management:
  security:
    enabled: false

spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/elenaaugusta94/configurations/config-repo
          
    
          
